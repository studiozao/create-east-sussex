# Legacy v1 — pre-Aug-2026 redesign backup

This folder is a snapshot of the site as it stood on `main` immediately
before the Aug 2026 bold-editorial redesign replaced it (quiet serif
editorial layout: Fraunces + Switzer, "Hacks" naming era copy, no video
hero, no interactive map).

It is kept here for reference only and is not linked from the live site.
The full history is also still in git — `git log main` before this backup
was taken — so this folder is a convenience snapshot, not the only copy.
